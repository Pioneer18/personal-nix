## Summary

- Add `home.activation.buildTachikomaUI` — runs `npm install` + `npm run build` in `mcps/tachikoma-ui/` on every rebuild (non-fatal)
- Add `home.activation.writeTachikomaUILauncher` — writes `~/.local/bin/tachikoma-ui-start`, a wrapper that sources `~/.secrets` then execs the Node server
- Declare `launchd.user.agents.tachikoma-ui` — `RunAtLoad = true`, `KeepAlive = true`, logs to `/tmp/tachikoma-ui.log`, UI available at http://localhost:4000

## Test plan

- [ ] `nix eval --impure .#homeConfigurations` exits without errors
- [ ] After `dev` rebuild, `~/.local/bin/tachikoma-ui-start` exists and is executable
- [ ] `launchctl list | grep tachikoma-ui` shows the agent loaded
- [ ] `curl -s http://localhost:4000` returns the app HTML
- [ ] `/tmp/tachikoma-ui.log` contains server startup output
- [ ] No secrets hardcoded in any nix file

🤖 Generated with [Claude Code](https://claude.com/claude-code)